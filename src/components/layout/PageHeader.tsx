/**
 * Stable import path requested by the product team.
 * Re-exports the canonical PageHeader from the workspace component set
 * so every page can use `@/components/layout/PageHeader`.
 */
export { PageHeader, QuickAction } from "@/components/workspace/PageHeader";
export type { } from "@/components/workspace/PageHeader";